const gameBodyWrapper = document.getElementById("game-body-wrapper");
const timerWrapper = document.getElementById("timer-wrapper");

const screenWidthPlusPadding = screen.width - 15;

gameBodyWrapper.style.width = screenWidthPlusPadding + "px";
gameBodyWrapper.style.height = screenWidthPlusPadding + "px";
timerWrapper.style.width = screenWidthPlusPadding + "px";